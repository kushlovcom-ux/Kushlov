# Expo / React Native resolve more reliably with a hoisted node_modules layout.
node-linker=hoisted
# Avoid interactive prompts in CI / EAS builds
confirmModulesPurge=false
# Ensure Babel presets/plugins resolve from the app package under pnpm
public-hoist-pattern[]=*babel*
public-hoist-pattern[]=*expo*

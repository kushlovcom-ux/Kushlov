import { createApp } from './src/app';

/**
 * Bundled into api/index.js by tsup — NOT a Vercel runtime entry file.
 * Lives outside src/ so .vercelignore can exclude src/ without breaking the build.
 */
export default createApp();

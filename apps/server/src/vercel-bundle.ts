import { createApp } from './app';

/**
 * Bundled into api/index.js by tsup — NOT a Vercel entry file.
 * (Do not name this server.ts; Vercel auto-deploys that as a broken second function.)
 */
export default createApp();

# EAS installs this app as a standalone project. Hoist so Metro/Babel
# can resolve Expo and React Native packages the same way the monorepo does.
node-linker=hoisted
confirmModulesPurge=false
public-hoist-pattern[]=*babel*
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*metro*
